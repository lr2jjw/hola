<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Importar el archivo de propiedades para usar en la configuración -->
    <context:property-placeholder location="classpath:application.properties" />


    <!-- Configuración del DataSource para Oracle -->
    <bean id="dataSourceOds" class="org.apache.commons.dbcp2.BasicDataSource">
        <property name="driverClassName" value="${datasource.driver-class-name}" />
        <property name="url" value="${datasource.url}" />
        <property name="username" value="${datasource.username}" />
        <property name="password" value="${datasource.password}" />
        <property name="initialSize" value="${datasource.initial-size}" />
        <property name="maxTotal" value="${datasource.max-total}" />
    </bean>

    <!-- Configuración del SessionFactory para Hibernate -->
    <bean id="sessionFactoryOds" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSourceOds"/>
        <property name="packagesToScan" value="com.bmv.mci.model"/>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${hibernate.dialect}</prop>
                <prop key="hibernate.show_sql">${hibernate.show-sql}</prop>
                <prop key="hibernate.hbm2ddl.auto">none</prop> <!-- Cambia según tus necesidades -->
                <prop key="hibernate.default_schema">${hibernate.default-schema}</prop>
            </props>
        </property>
    </bean>

    <!-- Configuración del TransactionManager para Hibernate -->
    <bean id="transactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactoryOds"/>
    </bean>

</beans>
